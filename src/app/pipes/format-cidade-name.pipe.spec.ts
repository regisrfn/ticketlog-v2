import { FormatCidadeNamePipe } from './format-cidade-name.pipe';

describe('FormatCidadeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCidadeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
